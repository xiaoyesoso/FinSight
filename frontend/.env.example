# Backend base URL for API calls in development.
# Set this so SSE streams bypass the Vite proxy (which buffers text/event-stream).
# In production, leave empty to use same-origin relative paths.
VITE_API_BASE_URL=http://localhost:8000
